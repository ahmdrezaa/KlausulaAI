# ── PDF Extraction ─────────────────────────────────────────────
pdfplumber==0.11.4          # Primary: terbaik untuk layout PDF UU

# ── Data Processing ────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4

# ── Text Processing ────────────────────────────────────────────
regex==2024.9.11            # Regex yang lebih powerful dari stdlib re

# ── Supabase / pgvector ────────────────────────────────────────
supabase==2.9.1             # Supabase Python client
vecs==0.4.3                 # pgvector via Supabase (untuk embedding)

# ── Embedding ─────────────────────────────────────────────────
# Pilih salah satu atau keduanya:
google-generativeai==0.8.3  # Google Gemini embedding (free tier)

# ── LangChain (untuk integrasi dengan Backend Engineer) ────────
langchain==0.3.7
langchain-community==0.3.7
langchain-google-genai==2.0.4  # Untuk Gemini

# ── Utilities ─────────────────────────────────────────────────
python-dotenv==1.0.1        # Load .env
tqdm==4.67.0                # Progress bar
rich==13.9.4                # Pretty terminal output (opsional tapi bagus)

# ── Development & Testing ──────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0

# ── Optional: Jupyter untuk exploratory analysis ──────────────
# jupyter==1.1.1
# ipython==8.29.0
